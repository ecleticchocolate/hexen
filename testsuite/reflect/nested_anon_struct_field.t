//@ expect val 41
struct Inner { i32 a }
struct Outer { Inner inner  u8 flag }
fn main() i32 {
    match Outer {
        struct { struct { X }; Y } { return (i32)sizeof(X)*10 + (i32)sizeof(Y) }
        else { return 99 }
    }
}
